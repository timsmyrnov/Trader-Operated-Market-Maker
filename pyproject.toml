[build-system]
requires = ['setuptools']
build-backend = 'setuptools.build_meta'

[project]
name = 'tomm'
version = '0.1.0'
description = 'Trader Operated Market Maker'
readme = 'README.md'
requires-python = '>=3.11'

[tool.setuptools]
package-dir = {'' = 'src'}

[tool.setuptools.packages.find]
where = ['src']
